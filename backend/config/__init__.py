"""
Legacy config package for arxiv-trend-predictor.

NOTE: New code should import from `backend.core` instead:

    from backend.core.config import settings
    from backend.core.constants import TECH_CATEGORIES, GENERIC_KEYWORDS

This module is kept only for backward compatibility during the refactor.
"""

